{
  "name": "swaad-sutra",
  "version": "1.0.0",
  "description": "Society Home Food Ordering - Swaad Sutra",
  "scripts": {
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "dev": "echo 'Run backend and frontend separately - see README'",
    "build": "cd frontend && npm run build && rm -rf ../backend/public && cp -r dist ../backend/public",
    "start": "cd backend && npm start"
  }
}
